tasks: []

done: []

chopping_block:
  disabled_systems:
    - Power-ups (BigPaddle, StickyPaddle) -- code built, PowerUpAssets registry empty and PowerUpManager.onBrickDestroyed returns early. See GAMEPLAY.md "Re-enabling Power-Ups".
    - ExplosiveBrick -- component + template exist, not referenced in any LevelConfig. See GAMEPLAY.md "Re-enabling Explosive Bricks".
  temp_scaffolding: []
  failed_attempts: []

notes:
  - All 11 LEVELS share the same physics config (ballSpeedMultiplier=1, paddleLerpFactor=0.88, ballSpeedIncrementPerBrick implied by Ball._speedBonus accumulator) — visual variety comes from grid + palette, not physics.
  - GameManager.maxLives defaults to 1 (@property on the scene component) — change in the editor, not in code.
  - Title screen is laid out as a special LevelConfig (`Title` const) with titleAnim=true to drive idle breathing pulse.
