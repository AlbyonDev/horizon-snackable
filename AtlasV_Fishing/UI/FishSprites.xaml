<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:horizon.ui_noesis;assembly=ui_noesis"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Background="Transparent" d:DesignWidth="540" d:DesignHeight="960">

  <!-- Stretch="Fill" + canvas dimensions bound to the screen aspect (set in TS)
       keeps the UI canvas perfectly aligned with the orthographic 3D view on
       any device aspect, with no letterboxing and no sprite distortion. -->
  <Viewbox Stretch="Fill">
    <Grid Width="{Binding canvasW}" Height="{Binding canvasH}">
      <local:DrawingSurface Width="{Binding canvasW}" Height="{Binding canvasH}"
                            Commands="{Binding drawCommands}" />
    </Grid>
  </Viewbox>
</Page>
