<Grid d:DesignWidth="1080" d:DesignHeight="1920"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:horizon.ui_noesis;assembly=ui_noesis"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <Grid.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Progress bar template with colored fill.
             Trick to get a "sliced capsule" look: the inner capsule is drawn
             at the FULL track width (so all 4 corners stay rounded), but it
             lives inside PART_Indicator which is only X% wide and clips it.
             Result: the visible fill is a capsule cut by a straight vertical
             edge at X%, and at 100% the full capsule shows through. -->
        <ControlTemplate x:Key="UpgradeProgressBarTemplate" TargetType="ProgressBar">
            <Grid x:Name="TrackPart">
                <Border Background="#0A1019" CornerRadius="4"/>
                <Border x:Name="PART_Indicator" HorizontalAlignment="Left" ClipToBounds="True">
                    <!-- Full-width capsule, clipped by PART_Indicator's width. -->
                    <Border Width="{Binding ActualWidth, ElementName=TrackPart}" HorizontalAlignment="Left" CornerRadius="4">
                        <Border.Background>
                            <SolidColorBrush Color="{Binding progressBarColor}"/>
                        </Border.Background>
                        <!-- Gradient shine overlay — same full-width capsule. -->
                        <Border CornerRadius="4">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                    <GradientStop Color="#50FFFFFF" Offset="0"/>
                                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                        </Border>
                    </Border>
                </Border>
            </Grid>
        </ControlTemplate>

        <!-- ItemContainerStyle to stretch items vertically -->
        <Style x:Key="StretchItemStyle" TargetType="ContentPresenter">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
        </Style>
    </Grid.Resources>

    <Viewbox VerticalAlignment="Center">
        <Grid Width="480" Height="850">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.15*"/>
                <RowDefinition Height="0.45*"/>
                <RowDefinition Height="0.40*"/>
            </Grid.RowDefinitions>
            <Grid>
                <Border Padding="8,6" VerticalAlignment="Stretch" Margin="0,0,0,-10">
                    <Border.Background>
                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                            <GradientStop Offset="0" Color="#FF161F2C"/>
                            <GradientStop Offset="1" Color="#00161F2C"/>
                            <GradientStop Offset="0.92665523" Color="#FF161F2C"/>
                        </LinearGradientBrush>
                    </Border.Background>
                </Border>
                <ItemsControl ItemsSource="{Binding slots}" HorizontalAlignment="Stretch" ItemContainerStyle="{StaticResource StretchItemStyle}" VerticalAlignment="Center" Margin="0,20,0,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="4" Rows="1"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#0F1A25" CornerRadius="6" Margin="3,4,3,0" Padding="6,4" BorderThickness="2" VerticalAlignment="Stretch" Visibility="{Binding isVisible, Converter={StaticResource BoolToVis}}">
                                <Border.BorderBrush>
                                    <SolidColorBrush Color="{Binding borderColor}"/>
                                </Border.BorderBrush>
                                <Grid VerticalAlignment="Stretch">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="16"/>
                                    </Grid.RowDefinitions>
                                    <Grid Grid.Row="0" VerticalAlignment="Center" Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Width="30" Height="30" CornerRadius="4" Margin="0,0,6,0">
                                            <Border.Background>
                                                <ImageBrush ImageSource="{Binding icon}" Stretch="Uniform"/>
                                            </Border.Background>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,-4,0">
                                            <TextBlock Text="{Binding title}" TextAlignment="Center" FontSize="13" FontWeight="SemiBold" Foreground="White" Stroke="Black" StrokeThickness="1"/>
                                            <TextBlock Text="{Binding value}" TextAlignment="Center"  FontSize="10" FontWeight="Bold" Stroke="Black" StrokeThickness="1" Margin="0,1,0,0" TextTrimming="CharacterEllipsis">
                                                <TextBlock.Foreground>
                                                    <SolidColorBrush Color="{Binding valueColor}"/>
                                                </TextBlock.Foreground>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                    <Grid Grid.Row="1" VerticalAlignment="Stretch" Visibility="{Binding progressVisible, Converter={StaticResource BoolToVis}}">
                                        <Border BorderBrush="Black" BorderThickness="1" CornerRadius="4">
                                            <Grid>
                                                <ProgressBar Value="{Binding progressPercent}" Minimum="0" Maximum="100" VerticalAlignment="Stretch" Template="{StaticResource UpgradeProgressBarTemplate}"/>
                                                <TextBlock Text="{Binding progressText}" FontSize="13" FontWeight="Bold" Foreground="White" Stroke="#444444" StrokeThickness="1" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
            <!-- Bar taking full width, fixed logical height -->
        </Grid>
    </Viewbox>
</Grid>
